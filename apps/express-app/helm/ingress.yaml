apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: turbo-express
  namespace: default
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
  - host: turbo.backend.marcmarina.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: turbo-express
            port:
              number: 80
