services:
  express-app:
    image: turbo-playground:latest
    command: node apps/express-app
    environment:
      PORT: 8080
    env_file:
      - apps/express-app/.env
    ports:
      - "8080:8080"
    deploy:
      replicas: 2
